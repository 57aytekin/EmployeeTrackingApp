<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.employeetrackingapp">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EmployeeTrackingApp">
        <activity
            android:name=".ui.process.AddProcessActivity"
            android:label="İşlem Ekle"
            android:exported="false" />
        <activity
            android:name=".ui.employee.AddEmployeeActivity"
            android:label="Personel ekle"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:label="Görev Takibi"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>